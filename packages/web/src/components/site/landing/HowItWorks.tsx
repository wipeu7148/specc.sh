import { motion } from "framer-motion";
import { useTranslation } from "react-i18next";

const STEPS = [
  {
    key: "step1" as const,
    num: "01",
    prompt: '"帮我做一个团队成员邀请功能，带邮件通知"',
    promptEn: '"Add a team invitation feature with email notification"',
  },
  {
    key: "step2" as const,
    num: "02",
    code: `// Auto-generated by Claude ✨
export const inviteRouter = router({
  send: protectedProcedure
    .input(InviteInputSchema)
    .output(InviteOutputSchema)
    .mutation(async ({ input, ctx }) => {
      return inviteService.send(input, ctx.userId);
    }),
});`,
  },
  {
    key: "step3" as const,
    num: "03",
    result: "✓ Types inferred · ✓ Lint passed · ✓ PR merged",
  },
] as const;

export function HowItWorks() {
  const { t } = useTranslation();

  return (
    <section
      id="howItWorks"
      className="relative py-24 sm:py-32 bg-zinc-50 dark:bg-zinc-900/50"
    >
      {/* Subtle gradient top */}
      <div className="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-zinc-200 dark:via-zinc-800 to-transparent" />

      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {/* Header */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.6 }}
          className="text-center mb-16"
        >
          <h2 className="text-4xl sm:text-5xl font-extrabold text-zinc-900 dark:text-white tracking-tight">
            {t("landing.howItWorks.sectionTitle")}
          </h2>
          <p className="mt-4 text-lg text-zinc-600 dark:text-zinc-400 max-w-2xl mx-auto">
            {t("landing.howItWorks.sectionSubtitle")}
          </p>
        </motion.div>

        {/* Steps */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 relative">
          {/* Connector line */}
          <div className="hidden lg:block absolute top-10 left-1/3 right-1/3 h-px bg-gradient-to-r from-blue-300 dark:from-blue-700 to-indigo-300 dark:to-indigo-700" />

          {STEPS.map((step, index) => (
            <motion.div
              key={step.key}
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: index * 0.15 }}
              className="relative"
            >
              {/* Step number */}
              <div className="flex items-center gap-3 mb-4">
                <div className="flex-shrink-0 w-10 h-10 rounded-full bg-gradient-to-r from-blue-600 to-indigo-600 text-white text-sm font-bold flex items-center justify-center shadow-lg shadow-blue-500/20">
                  {step.num}
                </div>
                <div className="h-px flex-1 bg-zinc-200 dark:bg-zinc-700 lg:hidden" />
              </div>

              {/* Card */}
              <div className="rounded-2xl border border-zinc-200 dark:border-zinc-800 bg-white dark:bg-zinc-900 p-6 h-full shadow-sm">
                <h3 className="text-lg font-semibold text-zinc-900 dark:text-white mb-2">
                  {t(`landing.howItWorks.steps.${step.key}.title`)}
                </h3>
                <p className="text-sm text-zinc-600 dark:text-zinc-400 mb-4 leading-relaxed">
                  {t(`landing.howItWorks.steps.${step.key}.desc`)}
                </p>

                {/* Visual hint for each step */}
                {step.key === "step1" && (
                  <div className="mt-4 rounded-lg bg-zinc-50 dark:bg-zinc-800 border border-zinc-200 dark:border-zinc-700 p-3">
                    <div className="flex items-start gap-2">
                      <span className="text-violet-500 mt-0.5 flex-shrink-0">
                        <svg
                          aria-hidden="true"
                          className="w-4 h-4"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          strokeWidth="2"
                        >
                          <path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 4v4l3 3" />
                        </svg>
                      </span>
                      <p className="text-xs text-zinc-500 dark:text-zinc-400 italic">
                        {step.prompt}
                      </p>
                    </div>
                  </div>
                )}

                {step.key === "step2" && (
                  <div className="mt-4 rounded-lg bg-zinc-950 dark:bg-black border border-zinc-800 p-3 overflow-x-auto">
                    <pre className="text-[11px] text-green-400 leading-relaxed whitespace-pre">
                      {step.code}
                    </pre>
                  </div>
                )}

                {step.key === "step3" && (
                  <div className="mt-4 space-y-2">
                    {(["typesInferred", "lintPassed", "prMerged"] as const).map(
                      (itemKey) => (
                        <div
                          key={itemKey}
                          className="flex items-center gap-2 text-xs text-emerald-600 dark:text-emerald-400 font-medium"
                        >
                          <svg
                            aria-hidden="true"
                            className="w-4 h-4 flex-shrink-0"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            strokeWidth="2.5"
                          >
                            <path
                              strokeLinecap="round"
                              strokeLinejoin="round"
                              d="M5 13l4 4L19 7"
                            />
                          </svg>
                          {t(`landing.howItWorksDemo.${itemKey}`)}
                        </div>
                      ),
                    )}
                  </div>
                )}
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}
